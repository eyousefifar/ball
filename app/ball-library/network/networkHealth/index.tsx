

export function registerNetworkEvents() {
  // EE.on('networkHealthCheck', checkNetwork);
  // NetInfo.addEventListener('connectionChange', onConnectionChange);
}

const onConnectionChange = (connectionInfo: ConnectionInfo) => {
  // if (connectionInfo.type === 'none') {
  //   checkNetwork();
  // } else {
  //   checkNetwork();
  // }
};


async function checkNetwork() {
  // try {
  //   const res: AxiosResponse = await axios.get('https://google.com', {
  //     timeout: 800
  //   });
  // } catch (error) {
  //   // Show no connection with a message
  // }

 // try {const res = await fetch('https://google.com/',{})}catch (e) {}
}
