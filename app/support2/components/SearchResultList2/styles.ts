import {StyleSheet} from 'react-native';

export default () => {
    const styles = StyleSheet.create({
        result_list: {
            width: '100%',
            paddingHorizontal: 8,
            marginBottom:8,
            paddingVertical:8
        },
        new_message_section:{

        }
    });

    return {styles};
};
