export interface expandedCardProps {
  address: string;
  location: {
    lat: number;
    long: number;
  };
  canBeCanceled: boolean;
}
export interface addressProps {
  address: string;
  location: {
    lat: number;
    long: number;
  };
}
